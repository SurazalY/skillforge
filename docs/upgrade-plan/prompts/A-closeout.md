# A 阶段收尾补充 Prompt

日期：2026-09-13。接收者：另一套 harness 中既有的 A 阶段主控。本 Prompt 接续已完成工作，不重启 A 全阶段。

以下正文可直接投放：

```text
你继续担任 SkillForge A 阶段主控。本次只完成 A 的基线与 Git 检查点收尾，不启动 B，不重跑已完成验收。

仓库：D:\Project\SkillForge_0912
WSL：/mnt/d/Project/SkillForge_0912

用户已接受任务分发 Agent 的建议，明确批准：
1. 补清是否已有可恢复的原版副本；仅有当前工作区与三个文件哈希不够。
2. 采用本地 Git 检查点；允许由你委派的执行子 Agent 进行必要的 git init、最小忽略规则调整、选择性暂存和首个基线提交。无需再次询问是否允许 init/commit。
3. 接受 A 报告的最小文档修正，任务分发 Agent 已更新增订和开发指南。原升级方案不改。

这项明确授权覆盖旧 Prompt/通用规则中“不得自行提交 Git”的默认条款，但仅适用于本次本地基线提交。你仍不得亲自搜索/阅读业务和测试代码、运行 Git、修改配置、编码、测试或操作原始证据；全部直接操作交执行子 Agent，你只维护文档、任务表与报告。不得推送远程、清理用户文件、重置工作区或扩大到 B。

先读最新 docs/upgrade-plan/01-addendum.md、02-development-guide.md、03-progress.md、04-controller-rules.md、phases/A-baseline.md，以及原 reports/A-stage-report.md。当前补充任务为 A-06。

请派发一个执行子 Agent 完成：

一、核实基线保存情况
- 先确认是否已有独立副本、归档或 Git 版本；有就报告实际位置和范围，不重复造快照。
- 说明当前源码与 A 已测版本的对应依据。若无法确认或发现后续变化，明确差异，不把新工作区冒称先前逐字节快照；不要自行撤销用户变化。
- 保存范围应涵盖恢复原版所需源码、依赖声明、适用测试/基线输入和必要文档。运行所需私有配置单独说明恢复条件，不能为了“完整”提交密钥。

二、建立本地 Git 检查点
- 按实际状态操作：如果仍无 Git，初始化本地仓库；如果期间已经建立，沿用并检查当前状态，不重复初始化或改写历史。
- 最小调整忽略规则，使需要的 docs/upgrade-plan 文档和基线输入能够入库；不要简单取消整棵 docs 的所有忽略规则或盲目强制添加整个目录。
- 排除 .env、凭据、运行临时目录、缓存和未经去敏的原始日志/抓包。保留这些本地文件，不删除、不移动来制造“干净工作区”。确认所暂存内容不含敏感信息，报告只给排除类别与结果，不回显秘密。
- 选择性暂存所需文件后创建本地基线提交，说明哪些文件属于原版业务内容，哪些是 A 文档、基线材料或本次 .gitignore 调整。
- 不修改业务实现，不修两项 Windows 夹具问题，不跑新付费 API，不推送远程。如果 Git 作者身份缺失，先检查已有合法配置；确实缺失则报告用户需要提供的姓名/邮箱，不捏造身份或擅改全局配置。

三、做一次比例适当的可恢复性核对
- 通过读取提交中的文件内容或向独立临时位置导出必要文件，确认源码、依赖声明及基线输入可取回，并核对与本次保存内容一致。
- 不覆盖工作区、不为验证提交重跑整套测试、不重复在线探测。
- 返回仓库路径、完整 commit SHA、保存与排除范围、恢复所需外部条件、与 A 已测版本的关系及核对结果。

你只审阅子报告；不足时让子 Agent 补充，不亲自打开代码/日志核查。

完成后更新 A-06 任务记录，在 docs/upgrade-plan/reports/A-stage-report.md 追加日期明确的“基线收尾补充”，保留原报告事实与历史。修正其中“基线已可恢复/可关闭”的结论所依据的事实，说明首个 Git 提交与后续文档变更的关系。

提交 SHA 写入报告后可能产生新的未提交文档差异，这是可明确记录的正常状态；不要为了让报告把自己的 SHA 包进同一提交而循环提交。必要时列出提交后仅文档更新的范围。

最终回复必须自足：是否已保存可恢复原版、commit、必要排除项、检查结果、是否仍有阻塞、是否建议关闭 A。没有发生的操作不能记为完成。

A 仍待用户最终关闭；不要自行将总进度改成 CLOSED，不启动 B，不重复派发 A-01—05。问题直接集中写在回复中并给推荐处理，不使用提问弹窗。
```
