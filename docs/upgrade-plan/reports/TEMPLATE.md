# 阶段报告：<阶段 ID 与名称>

> 模板，不是执行结果。保留有用字段，简单阶段可简写；不要用模板示例填充真实证据。

## 1. 结论与范围

- 主控标识、报告日期、仓库位置：
- 启动授权来源；采用的增订版本：
- 起始 commit、交付 commit 或未提交 diff 标识（由执行子 Agent 提供）：
- 建议：可提交用户关闭 / 需要补充 / 等待用户决定。
- 用户能观察到的结果：
- 本阶段实际完成范围；未完成范围：

## 2. 任务追踪

| 任务 ID | 状态 | 执行子 Agent | 交付摘要 | 子报告链接 | 未完成原因 |
|---|---|---|---|---|---|

## 3. 验收结果

| 原编号或增订验收 ID | 版本与场景 | 检查方法 | 结果 | 观察事实 | 证据位置 |
|---|---|---|---|---|---|

结果只使用 PASS / FAIL / INCONCLUSIVE / NOT_RUN / DEFERRED。DEFERRED 必须指明已批准的范围依据；不能把未做或失败写成不适用。

真实模型检查额外列出 provider、模型、协议、配置摘要、调用次数及费用口径；缺失字段写 unknown，不输出密钥。离线结构检查不能替代真实缓存收益。

## 4. 事实、风险与阻塞

说明失败原因及其对关闭条件的影响。区分原有问题、本阶段新增问题、外部环境限制。无需决定的相邻问题只简记，不自动扩展调查。

报告必须自足：即使读者不打开代码或日志，也能知道测试了什么、看到了什么、为什么支持或不支持关闭。

## 5. 需要用户决定

逐项写“问题—影响—推荐回答—不采纳代价”。无问题时写“无”。不要把常规实现选择转给用户；不得默认把等待回复视为批准。

## 6. 下一阶段交接

列出已冻结且下一阶段实际依赖的合同、状态版本、入口、限制、已知缺口和对应文档链接。不派发下一阶段工作，不给其状态标成已启动。

## 7. 文档与关闭申请

- 本阶段追踪表和开发记录位置：
- 本阶段仍在运行的子任务：应为无；否则解释并不得宣称完成。
- 请求用户决定是否关闭本阶段；关闭后由任务分发 Agent 更新总进度并提醒建立 Git 检查点。
