# 视觉与版式风格有限推测指南 (visual-style)

> [!IMPORTANT]
> **视觉分析限制说明**：由于源 Markdown 文件在转换时，丢失了全部图片和架构时序图（显示为 `picture omitted` 占位符）。本指南中关于页面版式、流程流转及 UI 截图的分析，**均是基于 Markdown 残留物理信息的有限推测**。在任何涉及系统精确架构拓扑、运行时时序以及实际终端高亮的场景下，**必须回看原始 PDF (`2-pico.pdf`) 以求证真实视觉设计**。

---

## 1. 基于残留信息的有限推测

虽然转换后的 Markdown 文件丢失了全部的直观图片，但通过大纲、文本占位符及表格结构，我们可以对其视觉版式做出以下有限推测：

* **高密度的信息表格化排版**
  原文大量使用了 Markdown 表格来对比多维数据或状态（例如：章节 00 的控制/状态/证据面映射表，章节 01 的工作区基线表，章节 06 的 Section 预算分配表等）。这推测原 PDF 正文具有高度结构化、网格化的排版风格，便于读者进行信息扫描。
* **图文互注的阐述逻辑**
  几乎每一个核心设计章节的前半部分都包含一张图片占位符（如第 2 章 runtime 控制环处的 `[516 x 669]` 图片占位符，第 3 章安全网关处的 `[516 x 355]` 占位符）。这表明原 PDF 习惯先通过流程图、时序图进行顶层解构，再在图下展开文本说明。
* **白板草稿与物理目录树的交织**
  章节 00 残留的 `.pico/` 树状目录框，表明文档除了正规架构图，还包含一些直观的存储目录层级树和局部伪代码块，呈现出技术演练现场的白板草稿感。

---

## 2. 核心视觉缺失与限制
* **流程与时序关系中断**：控制面/状态面/证据面的全局拓扑图、`Pico.ask()` 四步主循环时序图、以及安全网关的各级校验逻辑大图完全丢失。单纯阅读 Markdown 文本可能会在时序跳转和多端协作上产生理解卡顿。
* **界面截图信息缺失**：简历后面的 `运行部分截图` 占位符表明，原 PDF 带有终端控制台的实际运行图，但由于转换失败，无法从 Markdown 中获知其配色、高亮方式与控制台输出排版。

---

## 3. 对标与 PDF 回看触发条件

为了在编写新文档时还原对标原文档的视觉风格，后续 Agent **必须根据以下触发条件，打开原始 PDF (`2-pico.pdf`) 确认视觉排版**：

### 3.1. 触发条件清单

| 场景 | 需回看 PDF 确认的物理位置或要素 | 目的 |
| :--- | :--- | :--- |
| **画全局架构图时** | 查看 PDF 第 `00-全局总览` 章节中的全局架构图 (位于行 557 附近对应的 PDF 页面) | 确认控制面、状态面、证据面的模块框图和实虚线流向 |
| **画时序编排时** | 查看 PDF 第 `2-运行时主循环与执行编排设计` 章节中的 runtime 控制环 (位于行 1222 附近对应的 PDF 页面) | 确认 `Pico.ask()` 过程中，模型与工具、状态回写的时序跳转关系 |
| **画安全网关时** | 查看 PDF 第 `3-工具的接入与调用设计` 章节中的安全边界图 (位于行 1610 附近对应的 PDF 页面) | 确认校验、审批、只读拦截等安全网关的各级漏斗 |
| **设计控制台UI时** | 查看 PDF 简历与安装章节后面的 `运行部分截图` 页面 (位于行 108 附近对应的 PDF 页面) | 参考其终端界面高亮色彩、交互式 approval 询问的排版风格 |

### 3.2. 后续模仿设计建议
* **Mermaid 流程图绘制**：编写新文档时，涉及控制流、校验链的部分应优先对标原 PDF 风格。在 Markdown 中推荐直接使用 `Mermaid` 语法绘制拓扑或时序图，以保证视觉的连贯性。
* **高亮说明**：可对标原截图的配色思路，在文档中对关键运行日志（如 Approval 审批提示、逃逸拦截报错）进行加粗或颜色打标，强化视觉警示效果。
